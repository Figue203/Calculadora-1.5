else
                        {
                            Escribir.WriteLine(cadena);
                        }
                        cadena = Read.ReadLine();
                        if (encontrado == false)
                        {
                            Console.WriteLine("El producto no se encuentra en el registro");
                        }
                        Read.Close();
                        Escribir.Close();
